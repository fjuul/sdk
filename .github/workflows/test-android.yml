name: Test Android

on:
  push:
    paths:
      - android/**
      - .github/workflows/**

jobs:

  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Run Tests
      run: ./gradlew clean test
