apply plugin: 'java'

configure(subprojects) {
    apply plugin: 'java'

    repositories {
        jcenter()
    }

    test {
        testLogging {
            events "passed", "skipped", "failed", "standardOut", "standardError"
        }
    }
}